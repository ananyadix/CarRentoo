class Car {
  String model;
  double distance;
  double fuelCapacity;
  double pricePerHour;

  Car({
    required this.model,
    required this.distance,
    required this.fuelCapacity,
    required this.pricePerHour,
  });
}